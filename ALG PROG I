#ALGORITMOS E PROGRAMAÇÃO I

#Codaremos baseando-se em problemas reais.

#TEMAS ABORDADOS:

# Funções básicas (input(), print(), type(), string(), int(), float(), bool())

# Operaçoes Aritméticas (soma, subtração, multiplicação, divisão, divisão truncada, resto da divisão, potencia, radiciação, logaritmação)
# com o uso da biblioteca .math e com uso apenas dos operadores aritiméticos.

# Expressões Relacionais (==, !=, >, <, >=, <=)

# Expressões Lógicas (and, not, or)

# 
